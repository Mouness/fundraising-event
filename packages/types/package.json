{
    "name": "@fundraising/types",
    "version": "1.0.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc -w",
        "test": "vitest run",
        "test:cov": "vitest run --coverage"
    },
    "devDependencies": {
        "@vitest/coverage-v8": "^4.0.16",
        "typescript": "^5.0.0",
        "vitest": "^4.0.16"
    },
    "dependencies": {
        "class-validator": "^0.14.3"
    }
}